# apps/mvp-ppon-sub-backend.yaml
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: mvp-ppon-sub-backend
  namespace: default
spec:
  project: default
  source:
    repoURL: https://github.com/kt-ktk-03/GitOps.git
    targetRevision: main
    path: k8s/apps/mvp_ppon_sub_backend
  destination:
    server: https://kubernetes.default.svc
    namespace: default
  syncPolicy:
    automated:
      prune: true
      selfHeal: true