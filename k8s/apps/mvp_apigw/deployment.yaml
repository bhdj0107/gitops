apiVersion: apps/v1
kind: Deployment
metadata:
  name:  mvp-apigw
  namespace: default
  labels:
    app:  mvp-apigw
spec:
  selector:
    matchLabels:
      app: mvp-apigw
  replicas: 1
  template:
    metadata:
      labels:
        app:  mvp-apigw
    spec:
      imagePullSecrets:
        - name: acrsecret
      # initContainers:
        # Init containers are exactly like regular containers, except:
          # - Init containers always run to completion.
          # - Each init container must complete successfully before the next one starts.
      containers:
      - name:  mvp-apigw
        image:  acrteam03.azurecr.io/mvp_apigw:0e7b05542dedc395f5f52b6bf933fb7ea975f2fc

        # resources:
        #   requests:
        #     cpu: 100m
        #     memory: 100Mi
        #   limits:
        #     cpu: 100m
        #     memory: 100Mi
        # livenessProbe:
        #   tcpSocket:
        #     port: 80
        #   initialDelaySeconds: 5
        #   timeoutSeconds: 5
        #   successThreshold: 1
        #   failureThreshold: 3
        #   periodSeconds: 10
        # readinessProbe:
        #   httpGet:
        #     path: /_status/healthz
        #     port: 80
        #   initialDelaySeconds: 5
        #   timeoutSeconds: 2
        #   successThreshold: 1
        #   failureThreshold: 3
        #   periodSeconds: 10

        # env:
        # - name: DB_HOST
        #   valueFrom:
        #     configMapKeyRef:
        #       name: mvp-configmap
        #       key: DB_HOST
        ports:
        - containerPort: 8080
          name:  mvp-apigw
        # volumeMounts:
        # - name: localtime
        #   mountPath: /etc/localtime
      # volumes:
      #   - name: localtime
      #     hostPath:
      #       path: /usr/share/zoneinfo/Asia/Shanghai
      restartPolicy: Always
